//= require jquery
//= require jquery_ujs
// foundation
// turbolinks
// selectize

$(document).on('ready', function(){
    //var $home_form = $('#home-form')
    //var $home_form_selects = $home_form.find('select')
    //$home_form_selects.selectize({

    //})

    /*$('[id=my-select-country]').selectize({

    })

    $('[id=my-select]').selectize({

    })

    $('[id=quantity-select]').selectize({

    })

    $('[id=quantity-select]').each(function() {
        var $select = $(this)
        var selectize = $select.get(0)

        var value = selectize.selectize.$input.find('option').first().attr('value')
        selectize.selectize.setValue(value)
    })*/

    //$('select')

//    $('#select-state').selectize({
//        maxItems: 3
//    });
//
//    $('#select-country').selectize();
})